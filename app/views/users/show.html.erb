<div class="container-fluid">
	<%= render "layouts/main_navbar" %>
		<div class="row">
			<div class="col-md-12">
				<%= render 'answers' %>
			</div>
		</div>

		<div class="row" id="profile">
			<div class="det_pic">
			  <%= image_tag(@user.picture) if @user.picture? %>
			</div>
		</div>
		
		<div class="row">
			<div class="col-md-4 col-centered">
				<%= form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => {:method => :put, :multipart => true}) do |f| %>

					<%= devise_error_messages! %><!--Check to make sure this works-->
				
			  		<div class="form-group">
						<label><%= f.label :picture %></label><br />
						<%= f.file_field :picture, accept: 'image/jpeg,image/gif,image/png' %>
						<%= f.hidden_field :picture_cache %>
					</div>

					<div class="form-group">
			    		<%= f.submit "Update", class: "btn btn-primary" %>
					</div>
				
		  		<% end %>
			</div>
		</div>
			
		<script type="text/javascript">
		  $('#picture').bind('change', function() {
		    var size_in_megabytes = this.files[0].size/1024/1024;
		    if (size_in_megabytes > 5) {
		      alert('Maximum file size is 5MB. Please choose a smaller file.');
		    }
		  });
		</script>
			
		
		<div class="row">
			<div class="col-md-6 col-centered">
				<%= form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => {:method => :put, :multipart => true}) do |f| %>
		
					<div id="bio" name="user[bio]" class="text-center" >
						<%= sanitize @user.bio %>
					</div>
					
					<p id="btn-save" style="display: none;">
					   	<%= f.submit "Save", onclick: "saveRedactor();" %></button>
					</p>
		
				<% end %>
			</div>
		</div>
	
		<br>

		<div class="row"></div>
	
		<div class="row users" id="scenario">	
			<% User.list_answers(current_user).each do |scenario| %>
				<div class="col-md-4 well each_scenario">						
					<% scenario.each do |answer| %>							
						<p><%= answer %></p>
					<% end %>						
				</div>
			<% end %>
		</div>
			
		<div class="row users" id="outfit">
			<% @user.outfits.map do |outfit| %>
				<div class="col-md-4">
					<div class="small_outfit_canvas">
						<% outfit.items.map do |item| %>
							<div class="small_draggable2" data-item-id='<%= item.id %>' data-top='<%= item.closets.find_by(outfit_id: outfit.id).y_coordinate %>' data-left='<%= item.closets.find_by(outfit_id: outfit.id).x_coordinate %>' data-height='<%= item.closets.find_by(outfit_id: outfit.id).height %>' data-width='<%= item.closets.find_by(outfit_id: outfit.id).width %>'>
								<%= image_tag item.picture, class: "imageUpload" %>	
							</div>
						<% end %>
					</div>
				</div>
			<% end %>
		</div>
					  
</div>


