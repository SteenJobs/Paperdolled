
    <!-- Generated by jQuery -->
		<%= form_for [@answer] do |f| %>
  
  		<div class="form-group">
      		<label>Dress me for:</label>
			<%= select_tag :event_id, options_for_select(Event.all.map{ |option| [option.dress_me_for, option.id, { 'data-event-id' => "" }] }), {prompt: "Please select", class: 'form-control'} %>
		</div>
		<fieldset id="event-fieldset" disabled>
		<div class="form-group">
			<label>And:</label>
			<%= select_tag :option_id2, options_for_select(Option.all.where(question_type: "And:").map{ |option| [option.possible_response, option.id, { 'data-event-id' => option.event_id }] }), {prompt: "Please select", class: 'form-control'} %>
		</div>
		<div class="form-group">
			<label>Also:</label>
			<%= select_tag :option_id3, options_for_select(Option.all.where(question_type: "Also:").map{ |option| [option.possible_response, option.id, { 'data-event-id' => option.event_id }] }), {prompt: "Please select", class: 'form-control'}  %>
		</div>
		<div class="form-group">
			<label>With:</label>
			<%= select_tag :option_id4, options_for_select(Option.all.where(question_type: "With:").map{ |option| [option.possible_response, option.id, { 'data-event-id' => option.event_id }] }), {prompt: "Please select", class: 'form-control'} %>
		</div>
		<div class="form-group">
			<label>Date:</label>
			<input type="text" class="form-control" id="sandbox-container" name="answer[date]">
		</div>
		<div class="form-group">
			<label>Anything else?</label>
			<%= text_field :answer, :type_in, placeholder: "Type in...", class: "form-control" %>
		</div>
		<br>
	    <%= f.submit "Submit", class: "btn btn-default btn-block paperdolled-button" %>
	  </fieldset>
    <% end %>

